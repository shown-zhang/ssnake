{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(lldb) Launch",
      "type": "lldb", 
      "request": "launch",
      "program": "${workspaceFolder}/bin/snake-c",
      "args": [],
      "cwd": "${workspaceFolder}",
    }
  ]
}